# Copy this file to local.sh in the same folder and replace the examples.
# local.sh is ignored and loaded last by init.sh.

# Machine-specific drive navigation:
# Example navigation shortcuts for a Windows machine with data on E: drive:
# alias d='cd /e//Documents && ls -a'
# alias b='cd /e/Books && ls -a'
# alias D='cd /e/Downloads && ls -a'
# alias m='cd /e/Music && ls -a'
# alias vv='cd /e/Videos && ls -a'  # v is taken for vim
# alias r='cd /e/Repos && ls -a'

# A hard-coded application override belongs here only when env.local is not a
# better fit:
# alias special-tool='/e/Tools/special-tool.exe'
